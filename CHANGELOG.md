# 변경 기록

이 프로젝트는 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따릅니다.

VS Code 확장 `vscode-3d-model-lens` 에서 갈라져 나온 독립 포크이므로 버전은 **`0.1.0` 에서
새로 시작**합니다 — 확장의 `0.4.0` 을 잇지 않습니다. 다른 제품이기 때문입니다.

## [0.1.0] — 2026-08-29

첫 릴리스. VS Code 확장의 뷰어를 데스크톱 앱으로 옮겼습니다.

### 추가

- **뷰어** — glTF / GLB / STL 을 열고 바운딩 박스 `X / Y / Z` 를 표시. 정점 스냅 측정,
  내비게이션 큐브, 축 삼각대, Babylon Inspector. (확장에서 그대로 계승)
- **앱 셸** — 네이티브 메뉴, 열기 대화상자(⌘O), 드래그앤드롭, 최근 파일, 빈 창,
  새 창(⌘N). Finder 에서 `.gltf` / `.glb` / `.stl` 더블클릭으로 열기.
- **숫자키 카메라 단축키** — `0` 기본 위치 · `1` Top · `2` Front · `3` Right · `4` Back ·
  `5` Left · `6` Bottom. 같은 이동을 `View → Camera` 메뉴로도 할 수 있습니다.
- **설정** — 배경 · 그리드 · 소수 자릿수 · 시작 시 Inspector · 초기 단위. 모두 뷰어 패널에서
  바꿀 수 있고 사람이 읽을 수 있는 JSON 에 저장됩니다. 단위는 **파일별로 기억**합니다.

### 확장과 달라진 것

- 배경 모드 `theme` 이 따르는 대상이 VS Code 편집기 색에서 **OS 라이트/다크**로 바뀌었습니다.
- 측정이 폐기되는 경계가 "탭을 닫을 때"에서 **"다른 파일을 열거나 창을 닫을 때"**가 되었습니다.
  창은 배경으로 가도 파괴되지 않으므로 확장의 상태 복원 코드는 사라졌습니다.
- 창 하나에 모델 하나입니다. 동시에 여러 개를 보려면 새 창을 엽니다.

### 아직 없는 것

- Windows / Linux 빌드 — 검증할 환경이 없어 지원한다고 적지 않습니다.
- 코드 서명 · 공증 — 첫 실행 시 OS 경고가 뜹니다(README 의 우회 방법 참조).
- 자동 업데이트.
